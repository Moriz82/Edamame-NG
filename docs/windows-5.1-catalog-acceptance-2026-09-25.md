# Windows PowerShell 5.1 catalog acceptance — 2026-09-25

Disposable VM 1601 was a full clone of stopped Windows Server 2022 source guest on `lab-hypervisor`. The clone used temporary isolated bridge `lab-net-04`; the source and current assessment guests were not changed. QEMU Guest Agent ran the checks as SYSTEM, so this proves script behavior under Windows PowerShell 5.1, not standard-user escalation. The guest reported PowerShell `5.1.20348.4294`. Its clock was behind the Proxmox host, so guest run timestamps are identifiers rather than synchronized time evidence. No credentials or raw enumerator output are in this record.

The first direct `-Cve` and `-Poc` calls failed because `$PSScriptRoot` was empty while the `CatalogDir` parameter default was being bound. Moving that default assignment into the script body fixed both calls on PowerShell 5.1. Direct lookup then returned an indexed Windows candidate for `CVE-2021-36934` and a SHA-256-verified offline PoC reference for `CVE-2025-32463`. The catalog test now invokes both public commands and rejects a modified PoC file. Release/digest/cache, ACL/Resume-selection, and subprocess timeout tests also passed on this guest.

Run `20260925T092054Z-LAB-WKS01-2684` used four predownloaded assets with adjacent SHA-256 files. `tools.tsv` recorded all four as local and their digests. A 30-second per-tool test limit deliberately produced partial WinPEAS and PrivescCheck output; 241 and 28 candidate lines were alerted without credential values before the final raw filenames appeared. The isolated, domain-joined clone could not complete SharpHound collection; coverage recorded `partial-no-zip`, not a valid ZIP. `cve-index.tsv` contained only its header because these partial outputs supplied no CVE strings. The SYSTEM token recipe succeeded. Explicit and default Resume both reverified SYSTEM without enumeration. This run does not establish a Windows standard-user-to-SYSTEM path.

VM 1601, its ZFS disk, bridge `lab-net-04`, bridge-local transfer server, and staging files were removed. Final readback found no clone configuration, volume, bridge, or host staging file. Source guest remained stopped, non-test guests and other test guests remained running, and `labpool` reported healthy.
